name: Daily News Summary Reporter

on:
  schedule:
    # 04:30 UTC represents 07:30 TSI (Turkey Standard Time, UTC+3)
    - cron: '30 4 * * *'
  workflow_dispatch: # Allows manual trigger from GitHub UI

jobs:
  report:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
        cache: 'pip'

    - name: Install System Dependencies for Weasyprint
      run: |
        sudo apt-get update
        sudo apt-get install -y libpango-1.0-0 libharfbuzz0b libpangoft2-1.0-0

    - name: Install Python Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install feedparser requests beautifulsoup4 pydantic weasyprint google-generativeai

    - name: Run News Summary & Email Pipeline
      env:
        LLM_API_KEY: ${{ secrets.LLM_API_KEY }}
        GMAIL_USER: ${{ secrets.GMAIL_USER }}
        GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
      run: |
        python main.py
